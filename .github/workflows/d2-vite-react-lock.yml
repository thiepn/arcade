name: D2 Vite React Lock Refresh

on:
  push:
    branches: [d2/vite8-react6]

permissions:
  contents: write

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.4.0
      - run: bun add --dev 'vite@^8.2.2' '@vitejs/plugin-react@^6.1.0'
      - name: Commit lockfile refresh
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add package.json bun.lock
          if git diff --cached --quiet; then
            echo 'No dependency changes produced.'
            exit 1
          fi
          git commit -m 'Upgrade Vite to 8.2.2 and plugin-react to 6.1.0'
          git push
