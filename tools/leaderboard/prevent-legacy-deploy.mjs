console.error('The D1 Worker is legacy scoring-v2 reference code and is not compatible with leaderboard protocol 3. Deploy the Supabase backend described in docs/leaderboard/RELEASE.md.');
process.exit(1);
